---
title: 待测试
description: 当前版本已实现但仍需人工验证的变更项
---

# 待测试

## 商品套图工作台

- `/`、画布、生图、视频、提示词、素材和工作流等旧用户入口统一重定向到 `/product-suite`；商品页面隐藏原“无限画布”顶部导航，成为独立产品。
- 默认流程为“上传/追加 1-5 张多视角白底图或实拍图 → 填写可选商品事实和市场/品牌设置 → 选择生成类型 → 开始生成 → 单张处理或完整 ZIP”。
- 8 张固定覆盖电商白底主图、品牌主视觉、核心卖点图、真实使用场景、材质工艺特写、A+ 内容图、`3:2` 横版广告和 `2:3` 竖版社媒海报。
- 8 张使用不同镜头合同，主视觉失败不会阻断其他任务；主视觉后补成功时会免费回审已完成下游图片的整套风格。
- 5 张原图会按任务选择主图、匹配角度图，并生成包含全部 5 张的全角度联系表，避免代理 4 图上限丢失证据。
- 卖点图和 A+ 图由 Canvas 确定性渲染用户提供的商品事实；未填写时只使用“可见结构/原图信息”等非营销声明。
- 卖点/A+ 文案叠加完成后，再对最终交付图执行精确像素尺寸、有效成像、商品主色、主体轮廓、内部标识与部件、镜像、白底规范、任务重复和整套风格检查；本地 OCR 额外拦截原图和用户卖点中不存在的高置信新文字。
- 硬性失败不标完成；第二次请求会携带商品原图、全角度证据、失败稿和具体质检原因进行定向返修。
- 主体结构检查使用浏览器本地 U2-NetP 分割；OCR/U2-Net 临时不可用时结果标记 `inconclusive`，不自动重复扣费，也不能进入完整 ZIP，可免费重新质检。
- 每张支持普通重生成、免费重新质检和定向返修；停止或刷新中的上游任务标记“结果状态未知”，不会回到无警告待生成。
- 修改商品信息或市场/品牌设置后，旧结果立即失效。
- 仅固定 8 张全部 `pass` 时开放 ZIP；少选任务、`warning`、`inconclusive`、停止、失败或历史未质检结果均不能作为整套下载。
- 商品原图、设置、质检报告和结果保存在浏览器 IndexedDB；旧版已付费结果保留并提供免费新版质检，gotocc Key 不进入草稿或历史。
- gotocc 当前实测单张约 `$0.08`，8 张基础费用约 `$0.64`；仅明确的 425/429 响应自动重试，网络中断、5xx、参数、鉴权、超时、空响应和图片读取失败不会自动重试。
- 后端图片编辑代理仅接受 GPT Image 2、1-4 张 JPEG/PNG/WebP、单张不超过 15 MB、总请求不超过 64 MB，并强制 `n=1` 与 `b64_json` 响应。

## 2026-08-18 已完成验证

- gotocc `gpt-image-2 /v1/images/edits` 真实请求返回 `200` 和 1024×1024 图片，接口耗时约 89-100 秒。
- 摩托车原图生成的户外商业场景保留左右方向、车架、两只轮胎、红色涂装和主要贴花，并生成自然地面、接触阴影和匹配光照。
- 本地极简页经 Next.js `/api` 和 Go multipart 代理完成真实生成、候选显示及 PNG 下载。
- Go `handler` 测试通过，`router` 编译通过；Next.js 生产构建通过。
- 1440×900 桌面截图通过；390×844 CSS 视口 `body.scrollWidth=390`，用途卡片、连接按钮和高级入口均未越界。
- 已补齐 `video/page.tsx` 的既有缺失导入；全量 TypeScript 检查通过，生产构建不再忽略类型错误。

## 2026-08-19 已完成验证

- 生产环境真实生成摩托车 6 张套图；首轮 4 张成功、2 张因上游与网络波动失败，两个失败项单独重试后均成功，最终 ZIP 包含 6 张正确画幅 PNG。
- 品牌主视觉、材质特写、`3:2` 横版和 `2:3` 竖版共享黑红摄影棚、反射地面与光向；真实使用场景切换到山地用途，白底图保持电商用途。
- 6 张均保留摩托车方向、红色车架、两轮、主要贴花和部件关系。
- gotocc 当日实测 8 次请求（6 个初始任务 + 2 次失败重试）共增加 `$0.64`，即每次约 `$0.08`；失败请求也计入用量。
- 本地 Chrome 将上述真实摩托车结果回灌到 8 张新流程，U2-NetP 实际完成原图与所有结果的轮廓、结构和方向检查；合规的 8 张最终成品全部通过。
- 不合规卖点图（两件商品和大段规格文字）被结构门拦截；空白图连续两次失败后保留可见原因并锁住全部整套下载入口，单张恢复通过后才重新开放 ZIP。
- 质检自动返修请求已验证包含商品原图、品牌主视觉、失败稿、`TARGETED REPAIR` 和具体失败原因；卖点/A+ 图验证的是 Canvas 文案叠加后的最终交付图。
- 新版 `version: 5` 草稿刷新后可恢复 8/8、质检报告、模型原稿和最终成品；旧版完成结果会失效并要求按最新商品身份口径重新质检。

## 2026-08-19 本轮已完成验证

- 实际逆向 LinkFox `/draw/product-image-suite` 桌面页、公开教程和优秀案例，核对 1-5 张多角度输入、商品信息、品牌/市场、A+、主副图类型、比例、案例与历史结构。
- 新工作台 1440×900 与 390×844 截图通过；两个视口 `documentWidth === viewportWidth`，8 个任务齐全，移动端首屏提供结果/案例/历史图标入口。
- 全仓 `tsc --noEmit`、Next.js 生产构建（含 TypeScript）、Go `go test ./...` 和 11 条任务矩阵/ZIP 门禁断言通过。
- 浏览器空白首访没有 `/api/settings`、gotocc 生图请求、控制台错误或项目资源失败。
- 当前浏览器保存的 gotocc Key 真实检测返回 `401`，本轮无法执行新的付费 8 图对照；页面已自动清除失效连接状态。
- `ic` CNAME 已切换为 Cloudflare Proxied，SSL/TLS 模式为 `Full`；Cloudflare Trace 确认原 Cache Rule 命中，但 Render 的 Cloudflare for SaaS O2O 链路仍持续返回 `DYNAMIC`。
- 已部署 `product-suite-edge-cache` Worker，并以 `ic.xinglinhui.com/*` 精确路由接入生产；只缓存无查询参数的公开工作台 HTML、Next 静态文件、OCR、U2-Net、WASM 和案例资源。
- `/product-suite` 的 `v2-7cff0a8` 边缘缓存实测首次 `MISS`、第二次 `HIT`、`Age` 递增且 `rndr-id` 不变，边缘 TTL 为 24 小时；Next CSS 同样验证 `MISS → HIT`。
- `/api/gotocc/*`、查询参数、Cookie、RSC、Next 预取、鉴权、Range 和非 GET 请求全部绕过 Worker Cache；RSC 请求仍返回 `text/x-component`，生产页面控制台 0 报错。
